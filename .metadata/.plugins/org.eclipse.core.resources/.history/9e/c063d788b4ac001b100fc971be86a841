server:
    port: 8087

spring:
    application:
        name: ConfigServer
    cloud:
        config:
            server:
                git:
                  uri: https://github.com/dineshvishnugithub/SpringCloudConfigServer

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: http://localhost:8761/eureka/
    instance:
        hostname: localhost